diff --git a/lib/features/home/data/unified_complaints_map_repository_api.dart b/lib/features/home/data/unified_complaints_map_repository_api.dart
index 3b4135a..561e196 100644
--- a/lib/features/home/data/unified_complaints_map_repository_api.dart
+++ b/lib/features/home/data/unified_complaints_map_repository_api.dart
@@ -138,7 +138,6 @@ abstract final class UnifiedComplaintMapItemParser {
       locationDescription: _string(json['text_location']),
       categoryName: _string(category?['name']),
       municipalityName: _string(municipality?['name']),
-      reportsCount: _int(json['reports_count']) ?? 0,
       updatedAt: _date(json['updated_at']),
     );
   }
diff --git a/lib/features/home/domain/unified_complaints_map_repository.dart b/lib/features/home/domain/unified_complaints_map_repository.dart
index d9fb720..c855bed 100644
--- a/lib/features/home/domain/unified_complaints_map_repository.dart
+++ b/lib/features/home/domain/unified_complaints_map_repository.dart
@@ -12,7 +12,6 @@ class UnifiedComplaintMapItem {
     this.locationDescription,
     this.categoryName,
     this.municipalityName,
-    this.reportsCount = 0,
     this.updatedAt,
   });
 
@@ -25,7 +24,6 @@ class UnifiedComplaintMapItem {
   final String? locationDescription;
   final String? categoryName;
   final String? municipalityName;
-  final int reportsCount;
   final DateTime? updatedAt;
 
   bool get hasValidCoordinates =>
diff --git a/lib/features/home/presentation/screens/home_screen.dart b/lib/features/home/presentation/screens/home_screen.dart
index 0ab9600..144313d 100644
--- a/lib/features/home/presentation/screens/home_screen.dart
+++ b/lib/features/home/presentation/screens/home_screen.dart
@@ -439,6 +439,7 @@ class _MunicipalityMapCardState extends State<MunicipalityMapCard> {
         _items = items;
         if (!_items.contains(_selected)) _selected = null;
       });
+      _focusMapOnItems(items);
     } catch (_) {
       if (mounted) setState(() => _error = 'تعذر تحميل خريطة الشكاوى.');
     } finally {
@@ -446,6 +447,28 @@ class _MunicipalityMapCardState extends State<MunicipalityMapCard> {
     }
   }
 
+  void _focusMapOnItems(List<UnifiedComplaintMapItem> items) {
+    if (widget.mapOverride != null || items.isEmpty) return;
+
+    WidgetsBinding.instance.addPostFrameCallback((_) {
+      if (!mounted) return;
+      final points = items
+          .map((item) => LatLng(item.latitude, item.longitude))
+          .toList(growable: false);
+      if (points.length == 1) {
+        _mapController.move(points.single, 14.5);
+        return;
+      }
+      _mapController.fitCamera(
+        CameraFit.bounds(
+          bounds: LatLngBounds.fromPoints(points),
+          padding: const EdgeInsets.all(44),
+          maxZoom: 14.5,
+        ),
+      );
+    });
+  }
+
   List<UnifiedComplaintMapItem> get _visibleItems => _items.where((item) {
         return switch (_filter) {
           _MapComplaintFilter.all => true,
@@ -568,7 +591,7 @@ class _MunicipalityMapCardState extends State<MunicipalityMapCard> {
           key: ValueKey('home_unified_complaint_marker_${item.id}'),
           borderRadius: BorderRadius.circular(99),
           onTap: () => _select(item),
-          child: _ComplaintMarker(color: _statusColor(item.statusKey), selected: selected, reports: item.reportsCount),
+          child: _ComplaintMarker(color: _statusColor(item.statusKey), selected: selected),
         ),
       ),
     );
@@ -595,9 +618,9 @@ class _MapHeader extends StatelessWidget {
           child: Column(
             crossAxisAlignment: CrossAxisAlignment.start,
             children: [
-              Text('الشكاوى الموحدة على الخريطة', style: TextStyle(fontSize: 17, fontWeight: FontWeight.w900)),
+              Text('شكاوى المنطقة', style: TextStyle(fontSize: 17, fontWeight: FontWeight.w900)),
               SizedBox(height: 3),
-              Text('عرض حيّ للمشكلات المبلّغ عنها ضمن بلديتك.', style: TextStyle(color: _homeMutedText, fontSize: 12.5)),
+              Text('عرض مواقع الشكاوى الرئيسية ضمن بلديتك.', style: TextStyle(color: _homeMutedText, fontSize: 12.5)),
             ],
           ),
         ),
@@ -629,7 +652,7 @@ class _MapFilterBar extends StatelessWidget {
         children: [
           _MapFilterChip(label: 'جميع الحالات', selected: filter == _MapComplaintFilter.all, onTap: () => onChanged(_MapComplaintFilter.all)),
           const SizedBox(width: 8),
-          _MapFilterChip(label: 'قيد المتابعة', selected: filter == _MapComplaintFilter.active, onTap: () => onChanged(_MapComplaintFilter.active)),
+          _MapFilterChip(label: 'غير منتهية', selected: filter == _MapComplaintFilter.active, onTap: () => onChanged(_MapComplaintFilter.active)),
           const SizedBox(width: 8),
           _MapFilterChip(label: 'منتهية', selected: filter == _MapComplaintFilter.terminal, onTap: () => onChanged(_MapComplaintFilter.terminal)),
         ],
@@ -791,10 +814,9 @@ class _MapEmptyOverlay extends StatelessWidget {
 }
 
 class _ComplaintMarker extends StatelessWidget {
-  const _ComplaintMarker({required this.color, required this.selected, required this.reports});
+  const _ComplaintMarker({required this.color, required this.selected});
   final Color color;
   final bool selected;
-  final int reports;
 
   @override
   Widget build(BuildContext context) {
@@ -821,18 +843,6 @@ class _ComplaintMarker extends StatelessWidget {
             ),
             child: const Icon(Icons.location_on_rounded, color: Colors.white, size: 20),
           ),
-          if (reports > 1)
-            PositionedDirectional(
-              top: -4,
-              end: 0,
-              child: Container(
-                constraints: const BoxConstraints(minWidth: 19, minHeight: 19),
-                alignment: Alignment.center,
-                padding: const EdgeInsets.symmetric(horizontal: 4),
-                decoration: BoxDecoration(color: Colors.white, shape: BoxShape.circle, border: Border.all(color: color, width: 1.5)),
-                child: Text('$reports', translate: false, textDirection: TextDirection.ltr, style: TextStyle(color: color, fontSize: 9.5, fontWeight: FontWeight.w900)),
-              ),
-            ),
         ],
       ),
     );
@@ -904,10 +914,5 @@ bool _isTerminal(String key) => switch (key.toLowerCase()) {
       _ => false,
     };
 
-Color _statusColor(String key) => switch (key.toLowerCase()) {
-      'resolved' || 'closed' => AppColors.success,
-      'rejected' => _homeDanger,
-      'submitted' || 'in_progress' || 'under_review' => const Color(0xFF277AA8),
-      'draft' => const Color(0xFFA26D18),
-      _ => _homeDeepGreen,
-    };
+Color _statusColor(String key) =>
+    _isTerminal(key) ? AppColors.success : _homeDanger;
diff --git a/lib/l10n/app_localizations.dart b/lib/l10n/app_localizations.dart
index defaabd..50fae1c 100644
--- a/lib/l10n/app_localizations.dart
+++ b/lib/l10n/app_localizations.dart
@@ -192,8 +192,12 @@ class AppLocalizations {
     'الشكاوى الموحدة على الخريطة': 'Unified complaints on the map',
     'عرض حيّ للمشكلات المبلّغ عنها ضمن بلديتك.':
         'A live view of reported issues in your municipality.',
+    'شكاوى المنطقة': 'Area complaints',
+    'عرض مواقع الشكاوى الرئيسية ضمن بلديتك.':
+        'View locations of primary complaints in your municipality.',
     'جميع الحالات': 'All statuses',
     'قيد المتابعة': 'Active',
+    'غير منتهية': 'Open',
     'منتهية': 'Closed',
     'إعادة تحميل الخريطة': 'Reload map',
     'جارٍ تحميل الشكاوى الموحدة...': 'Loading unified complaints…',
diff --git a/test/features/home/home_screen_test.dart b/test/features/home/home_screen_test.dart
index af48123..7bdac67 100644
--- a/test/features/home/home_screen_test.dart
+++ b/test/features/home/home_screen_test.dart
@@ -14,7 +14,6 @@ void main() {
         longitude: 36.2765,
         statusKey: 'in_progress',
         statusName: 'قيد المعالجة',
-        reportsCount: 3,
       ),
       const UnifiedComplaintMapItem(
         id: 12,
diff --git a/test/features/home/unified_complaints_map_repository_test.dart b/test/features/home/unified_complaints_map_repository_test.dart
index aa58885..01960ef 100644
--- a/test/features/home/unified_complaints_map_repository_test.dart
+++ b/test/features/home/unified_complaints_map_repository_test.dart
@@ -10,7 +10,6 @@ void main() {
       'text_location': 'شارع بغداد',
       'latitude': '33.5192',
       'longitude': 36.2923,
-      'reports_count': '4',
       'updated_at': '2026-08-15T08:30:00.000000Z',
       'current_status': <String, dynamic>{
         'key': 'in_progress',
@@ -24,7 +23,6 @@ void main() {
     expect(item.latitude, closeTo(33.5192, .00001));
     expect(item.longitude, closeTo(36.2923, .00001));
     expect(item.statusKey, 'in_progress');
-    expect(item.reportsCount, 4);
     expect(item.hasValidCoordinates, isTrue);
   });
 
diff --git a/test/features/settings/settings_language_test.dart b/test/features/settings/settings_language_test.dart
index a67a1d6..12e9566 100644
--- a/test/features/settings/settings_language_test.dart
+++ b/test/features/settings/settings_language_test.dart
@@ -88,8 +88,8 @@ void main() {
       'Showing 15 of 33 complaints.',
     );
     expect(
-      english.translate('الشكاوى الموحدة على الخريطة'),
-      'Unified complaints on the map',
+      english.translate('شكاوى المنطقة'),
+      'Area complaints',
     );
     expect(
       english.translate('إعادة تحميل الخريطة'),
