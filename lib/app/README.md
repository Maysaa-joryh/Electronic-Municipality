Project app bootstrap and routing will live under `lib/app/`.

This workspace already contains a large `main.dart` used for visual reference.
The new files here provide a gradual migration path and contracts for backend
integration (fake implementations live under `features/*/data`).

Follow the TODOs in these files when replacing the single-file demo.
